/* eslint-disable no-underscore-dangle */
/* eslint-disable no-undef */

// import fss from "@absolunet/fss"
// import path from "path"

if (typeof $ == undefined) {
  $ = {}
}

$.functions = {}

function init(emote) {
  // const emoteFolder = path.join("E:", "Emote Projects", emote)
  // const aiFile = path.join(emoteFolder, `${emote}.ai`)
  // const aiFileExists = fss.pathExists(aiFile)
  // if (aiFileExists) {
  //   app.open(new File(aiFile))
  //   return true
  // }
  // const document = app.documents.add(DocumentColorSpace.RGB)
  // const newLayer = document.layers.add()
  // newLayer.name = "Test"
  // const draftFile = path.join(emoteFolder, "draft.png")
  // const draftFileExists = fss.pathExists(draftFile)
  // if (draftFileExists) {
  //   const draftLayer = document.layers.add()
  //   draftLayer.name = "Draft"
  // }
  // document.save(new File(aiFile))
  // return true
}

// $.functions = {
//   init,
// }

$.ready = 1